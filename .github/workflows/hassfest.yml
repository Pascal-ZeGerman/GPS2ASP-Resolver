name: hassfest

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  hassfest:
    name: Validate manifest.json
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: "home-assistant/actions/hassfest@master"
