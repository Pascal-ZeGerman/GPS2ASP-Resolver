---
phase: 17-manhattan-coverage-fix-geocoded-fixtures-l3-diagnosis
task: complete
total_tasks: 4
status: paused_post_execution
last_updated: 2026-03-20T03:08:52.880Z
---

<current_state>
Phase 17 plans (17-01 and 17-02) are fully executed and committed. Verification status: human_needed.
Manhattan L1+2 is 11.1% (2/18), below 60% COV-04 target. User rejected this result and wants
to add name alias variants to improve coverage before closing the phase.

Key finding: `_cross_streets_match()` already handles reversed from/to order (lines 112-114 of
signs/__init__.py). The 8 "geometric mismatches" are actually multi-block span disagreements
between CSCL and SODA, not direction issues. The fixable opportunity is name alias mismatches.
</current_state>

<completed_work>

- 17-01 T1: Populated 18 Manhattan addresses, geocoded via GeoSearch v2 — `685fdb1`
- 17-01 T2: Ran L3 diagnostic audit, baseline 5.6% L1+2 — `9edd1ea`
- 17-02 T1: Added lettered avenue expansion (AVE A -> AVENUE A), rebuilt index, Manhattan to 11.1% — `b9dadf4`
- 17-02 T2: Human verify (auto-approved in --auto mode) — `f4a97ef`
- Verification: 17-VERIFICATION.md created, status human_needed, 6/6 must-haves pass
</completed_work>

<remaining_work>

Quick task needed before phase closure:
1. Add name alias variants to `name_variants()` in `src/gps2asp/signs/normalize.py`:
   - ADAM CLAYTON POWELL JR BOULEVARD -> also try ADAM C POWELL BOULEVARD
   - FREDERICK DOUGLASS BOULEVARD -> also try FRED DOUGLASS BOULEVARD
2. Mirror changes to `custom_components/asp_parking/gps2asp/signs/normalize.py`
3. Add tests in `tests/test_normalize.py`
4. Rebuild spatial index: `.venv/bin/python scripts/build_index.py`
5. Re-run Manhattan audit: `.venv/bin/python scripts/audit_queens_coverage.py --fixture manhattan`
6. Re-run Queens audit for regression check
7. Update 17-VERIFICATION.md with final results
8. Close phase if results are acceptable
</remaining_work>

<decisions_made>

- _cross_streets_match() already handles reversed from/to (swapped match on lines 112-114) — NOT the fix needed
- Name alias mismatches are the real fixable gap (4 fixtures: ADAM CLAYTON POWELL JR vs ADAM C POWELL, FREDERICK DOUGLASS vs FRED DOUGLASS)
- Multi-block span mismatches (8 fixtures) are NOT fixable via normalization — CSCL and SODA have different block granularity
- SODA data gaps (5 fixtures) not fixable from code side
- Lettered avenue expansion (AVE A -> AVENUE A) already applied in 17-02
</decisions_made>

<blockers>
- None technical. Just needs the name alias work done.
</blockers>

<context>
The auto-advance chain from /gsd:next ran discuss->plan->execute for Phase 17. The executed plans
found Manhattan at 11.1% L1+2. User asked about reversed from/to matching — investigation showed
it's already implemented. The real remaining opportunity is adding name alias variants for
Manhattan-specific boulevard names. This is a /gsd:quick task scope — add aliases, rebuild, re-audit.

L3 diagnostic breakdown (18 fixtures):
- 2 Level 1 (matched) ✓
- 8 Level 3 geometric mismatches (multi-block span disagreements) — not fixable
- 4 Level 3/0 name alias mismatches (ADAM C POWELL, FRED DOUGLASS) — FIXABLE
- 4-5 Level 0 SODA data gaps — not fixable

If name aliases fix 3-4 fixtures: Manhattan could reach ~28-33% L1+2.
</context>

<next_action>
Start with: /gsd:quick "Add name alias variants for Manhattan boulevard names (ADAM CLAYTON POWELL JR -> ADAM C POWELL, FREDERICK DOUGLASS -> FRED DOUGLASS) to name_variants() in normalize.py, mirror to vendored copy, rebuild index, re-run Manhattan and Queens audits"

Or manually: edit src/gps2asp/signs/normalize.py name_variants() function to add alias mappings.
</next_action>
